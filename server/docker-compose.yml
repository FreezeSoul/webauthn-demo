services:
  wa_db:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: webauthn
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - wa_data:/var/lib/postgresql

volumes:
  wa_data:
